# Matrix dotfiles - Appearance (low-spec tuned, caelestia-inspired palette)
# Target: Intel UHD 605 iGPU, 4GB RAM. No blur, no shadows, fast animations.
# Palette (see DOC.md): deep navy surfaces + soft sky-blue accent.

$accent = rgba(7ab3f5ee)
$accent2 = rgba(4a6fa5ee)
$surface = rgba(1d243066)

general {
    gaps_in = 4
    gaps_out = 8
    border_size = 2
    col.active_border = $accent $accent2 45deg
    col.inactive_border = $surface
    layout = dwindle
    resize_on_border = true
}

decoration {
    blur {
        enabled = false
    }

    shadow {
        enabled = false
    }

    # Rounded corners, caelestia-style — pure geometry, no blur cost
    rounding = 8
}

animations {
    enabled = true

    # Short durations (2-3) to mask low framerates on weak iGPUs
    bezier = overshot, 0.05, 0.9, 0.1, 1.05
    bezier = smoothOut, 0.36, 0, 0.66, -0.56
    bezier = smoothIn, 0.25, 1, 0.5, 1

    animation = windows, 1, 3, overshot, slide
    animation = windowsOut, 1, 3, smoothOut, slide
    animation = border, 1, 3, default
    animation = fade, 1, 2, smoothIn
    animation = workspaces, 1, 3, overshot, slide
}

dwindle {
    pseudotile = true
    preserve_split = true
}

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
    disable_splash_rendering = true
}
