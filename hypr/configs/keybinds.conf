# Matrix dotfiles - Keybinds
# Explicit dependencies: kitty, rofi, wpctl, brightnessctl, grim, slurp
# Optional (windowrules only): pavucontrol, thunar

$terminal = kitty
$launcher = rofi -show drun
$files = thunar
$displays = ~/.config/hypr/scripts/multi-display.sh

$mod = SUPER

# Applications
bind = $mod, Return, exec, $terminal
bind = $mod, D, exec, $launcher
bind = $mod, E, exec, $files

# Window management
bind = $mod, Q, killactive
bind = $mod, F, fullscreen, 0
bind = $mod, Space, togglefloating
# Multi-display mode picker: PC only / Duplicate / Extend / Second screen only
bind = $mod, P, exec, $displays
bind = $mod, J, layoutmsg, togglesplit

# Focus
bind = $mod, left, movefocus, l
bind = $mod, right, movefocus, r
bind = $mod, up, movefocus, u
bind = $mod, down, movefocus, d

# Workspaces
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9

# Move window to workspace
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9

# Mouse
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# Volume (wpctl) - "Âm lượng"
binde = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 5%+
binde = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

# Brightness (brightnessctl)
binde = , XF86MonBrightnessUp, exec, brightnessctl set 5%+
binde = , XF86MonBrightnessDown, exec, brightnessctl set 5%-

# Screenshots (grim + slurp)
$screenshot_dir = ~/Pictures/Screenshots
bind = , Print, exec, mkdir -p $screenshot_dir && grim $screenshot_dir/$(date +%Y-%m-%d_%H-%M-%S).png
bind = SHIFT, Print, exec, mkdir -p $screenshot_dir && grim -g "$(slurp)" $screenshot_dir/$(date +%Y-%m-%d_%H-%M-%S).png

# Session
bind = $mod, M, exit,
